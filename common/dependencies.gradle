dependencies {
    implementation(libs.balmCommon) {
        changing = libs.versions.balm.get().endsWith("SNAPSHOT")
    }
    implementation(libs.kumaCommon)

    implementation("net.createmod.ponder:Ponder-Common-${libs.versions.minecraft.get()}:${libs.versions.ponder.get()}")
}
